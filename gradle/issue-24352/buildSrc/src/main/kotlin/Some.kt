// run and then comment `internal` and then run again with info logging enabled
// it shows `Skipping Kotlin DSL script compilation (Project/TopLevel/stage2) as it is up-to-date.`
// it should recompiled instead
internal
interface Some
